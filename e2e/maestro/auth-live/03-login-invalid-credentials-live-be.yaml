appId: org.reactjs.native.example.FIXYZMob
---
- launchApp:
    clearState: true
    clearKeychain: true
    arguments:
      mobApiBaseUrl: ${LIVE_API_BASE_URL}
      mobDisableAnimations: true
- extendedWaitUntil:
    visible:
      id: login-username
    timeout: 20000
- tapOn:
    id: login-username
- waitForAnimationToEnd:
    timeout: 5000
- inputText: ${LIVE_EMAIL}
- tapOn:
    id: login-password
- waitForAnimationToEnd:
    timeout: 5000
- inputText: ${LIVE_INVALID_PASSWORD}
- pressKey: Enter
- extendedWaitUntil:
    visible: "아이디 또는 비밀번호가 올바르지 않습니다."
    timeout: 10000
- assertVisible:
    id: login-submit
